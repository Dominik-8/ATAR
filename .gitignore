__pycache__/
*.pyc
.atar/
build/
dist/
*.egg-info/
venv/
.venv*/
.env
# generated artifacts (keep out of the repo) — root-scoped so they can
# never swallow same-named fixtures under tests/vectors/ (this bit CI twice)
/dashboard_demo.html
/atar-dashboard.html
/vouch*.json
/*.vouch.json
/agent-card.json

# rotation artifacts (committed by accident, generated by atar rotate/reissue)
/rotation.json
/reissued.json
